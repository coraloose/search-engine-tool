# Search Engine Tool

A Python command-line search engine tool for crawling `https://quotes.toscrape.com/`, building an inverted index, and searching pages by keywords.

## Setup

```bash
pip install -r requirements.txt